 /*const filteredName = data.map((country,index) => {
          return country.name.common.toLowerCase()
     })

     console.log(filteredName);*/

     

     /*const exampleName = data.map((country,index) => {
          return [country.name.common.toLowerCase(),index]
     })

     console.log(exampleName);
     console.log(data[0].population);
     console.log(data[0].capital);
     console.log(Object.values(data[0].languages));
     console.log(data[0].flags);

     const inputNameValue = filteredName.filter((name) => {
          return input.toLowerCase() === name.slice(0,input.length)
     })

     const listedCountry = inputNameValue.map((country)=>{
          return <li key={country} >{country}</li>
     })

 return <div>
 <div>
      find countries: <input type="text" value={input} onChange={(e) => setInput(e.target.value)}  />
 </div>
     <ul>
          {!input 
          ? null:
          inputNameValue.length > 10 
          ? <p>Too many matches, specify another filter</p>
          : listedCountry }
     </ul>

 </div>
*/